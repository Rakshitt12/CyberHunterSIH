# Backend URL used ONLY as the server-side /api/* rewrite destination
# (see next.config.mjs). The browser always calls relative /api/* paths.
# Local dev: http://localhost:8001
# Vercel production env: https://cyberhuntersih.onrender.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001

